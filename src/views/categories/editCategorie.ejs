<%- include('../partials/header') %>
<body>
    <%- include('../partials/navbar') %>
    <div class="container">
        <hr>
        <div class="card">
            <div class="card-header">
                <h2>Edicão de categoria</h2>
            </div>
            <div class="card-body">
                <form action="/admin/categories/update" method="POST">
                    <input class="form-control" type="text" name="title" value="<%= response.title %>" placeholder="Nome da categoria" required=true>
                    <input name="id" type="hidden" value="<%= response.id_categorie %>">
                    <br>
                    <button class="btn btn-outline-success">Atualizar</button>
                </form>
            </div>
            </div>
        </div>
</body>
<%- include('../partials/footer') %>